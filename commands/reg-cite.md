---
description: Produce a publication-quality citation (Variant A inline + Variant B block) for a Pakistan lender regulation, with page deep link, section anchor, verbatim quote, wikilink and chunk ID.
---

Use the **pakistan-regs-cite** skill to produce both inline and block citations for the regulation / topic below.

Verify against `data/chunks.jsonl` (chunk_id, source_url_with_page, section_anchor, verbatim text). Refuse to fabricate — if you cannot find the chunk, say so and stop.

Target: $ARGUMENTS
